**请根据我上传的参考图片，提取其视觉风格，并生成一张完整的 Design System 规范信息图。**

要求：
以我上传的图片为唯一视觉参考，分析并复刻它的整体视觉语言，包括但不限于：色彩、字体气质、版式结构、装饰元素、边框样式、图形语言、材质纹理、插画/摄影风格、组件样式、信息层级与排版节奏。

请生成一张竖版高清信息图，内容是“该参考图的设计系统规范说明”，而不是重新绘制原图本身。

信息图需要包含以下模块：

1. **标题区**

   * 标题为：`Design System`
   * 中文主标题根据参考图主题自动命名，例如：`XXX 公众号文章视觉规范`
   * 标题风格要完全贴合参考图的视觉气质。

2. **颜色系统**

   * 从参考图中提取主色、强调色、背景色、卡片色、边框色、正文色、辅助色。
   * 每个颜色显示色块、中文名称和 HEX 色值。
   * 色彩命名要符合参考图的氛围。

3. **字体与排版系统**

   * 分析参考图中的标题、副标题、正文、注释、数字编号等文字层级。
   * 展示字号、字重、行高、字距、字体气质。
   * 如果无法识别具体字体，请用相近字体风格描述，例如：衬线体、黑体、手写体、复古宋体、现代无衬线等。

4. **版式结构**

   * 拆解参考图的页面布局。
   * 版式结构必须按微信公众号图文阅读规格设计：正文内容区最大宽度以 `578px` 为基准，不要设计成桌面网页宽屏布局。
   * 展示内容最大宽度、边距、栅格、卡片间距、模块分区、对齐方式，并明确标注 `max-width: 578px`。
   * 用简洁的线框图说明整体结构。
   * 中间栏必须展示一张“公众号长文页面示例”竖图，像真实微信公众号文章截图一样呈现完整的纵向阅读页面；左右两侧再展示颜色、字体、组件、tokens 等规范说明。

5. **组件系统**

   * 提取参考图中的常见组件样式，例如：标题卡片、内容卡片、编号章、标签、按钮、分割线、Callout、图片容器、图标、装饰边框等。
   * 每个组件需要给出视觉说明和适用场景。

6. **图像 / 插画风格**

   * 总结参考图中的图像风格，例如：摄影感、插画感、颗粒感、复古感、极简感、科技感、手绘感、拼贴感等。
   * 展示一组小型风格示例图标或插画元素。

7. **设计 Tokens**

   * 在底部总结颜色、圆角、阴影、间距、边框、纹理等设计 token。
   * 使用清晰的小卡片形式排列。

视觉要求：

* 整张图必须延续我上传参考图的视觉风格。
* 不要使用现代通用 UI 模板风格。
* 不要改变参考图的核心审美。
* 不要凭空加入与参考图风格冲突的元素。
* 版式要像一张专业设计规范海报。
* 整体信息图的核心应用场景是微信公众号长文视觉系统：中间栏保留一张竖向公众号页面示例，页面正文宽度按 `578px` 最宽规格表达。
* 信息密度高，但层级清晰。
* 中文排版优先，文字真实可读，不要乱码。
* 输出为竖版长图，适合社交媒体发布。
* 高清、精致、完整、设计感强。

最终输出：
一张基于我上传参考图片风格提炼而成的 Design System 规范信息图。
